import 'package:flutter/material.dart';

class CarNotesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Car Notes Screen'),
      ),
      body: Center(
        child: Text('Car Notes'),
      ),
    );
  }
}
