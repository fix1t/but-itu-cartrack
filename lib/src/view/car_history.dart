import 'package:flutter/material.dart';

class CarHistoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Car History Screen'),
      ),
      body: Center(
        child: Text('Car History'),
      ),
    );
  }
}
